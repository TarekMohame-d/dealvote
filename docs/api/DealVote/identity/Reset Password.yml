info:
  name: Reset Password
  type: http
  seq: 2

http:
  method: POST
  url: "{{host}}/identity/reset-password"
  body:
    type: json
    data: |-
      {
        "email": ""
      }

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
